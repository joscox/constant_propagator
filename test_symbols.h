#ifndef SYMBOLS_H
#define SYMBOLS_H

#include <math.h>

enum symbols{
	BAD, NUM, COND, VAR, OP, ELSE, EOL
};
char *symbolnames[] = {"BAD", "NUM", "COND", "VAR", "OP", "ELSE", "EOL"};

 
#endif //SYMBOLS_H
